import React from 'react';

import './PrimaryButton.scss';

function PrimaryButton(): React.ReactElement {
  return (
    <button type="submit" className="button primary">Test</button>
  );
}

export default PrimaryButton;
